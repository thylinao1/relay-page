# RELAY project page

A single-file project page for RELAY, the transhipment connection guardian built for
PSA Code Sprint 2.0. Open `index.html` in a browser; there is no build step and no
dependency beyond the web fonts it requests.

`assets/` holds the figures, the console stills and the twenty-second console clip the
page embeds. All terminal data shown is synthetic.
